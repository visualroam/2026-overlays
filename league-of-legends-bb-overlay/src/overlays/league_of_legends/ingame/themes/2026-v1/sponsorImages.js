/**
 * Rotating sponsor art when no inhibitor respawn is active.
 * Use public/ paths (e.g. "/sponsors/logo.png") or full https URLs.
 */
export const SPONSOR_IMAGES = [
  "/sponsors/example-1.png",
  "/sponsors/example-2.png",
];

/** Display each sponsor slide for this long (ms). */
export const SPONSOR_ROTATION_MS = 8000;
